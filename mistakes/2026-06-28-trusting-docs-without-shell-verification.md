# 误把文档约定当成运行时事实

## 错误

看到 `RTK.md` 明确要求使用 `rtk`，就默认当前 shell 也一定可用。

## 影响

- 容易在第一步就卡住。
- 让排障从“验证环境”变成“解释文档”，浪费时间。

## 修正

- 先执行最小验证。
- 若命令不存在，直接接受事实并切换备用路径。
- 把“文档要求”和“实际可用”分开记录。

## 可复用提醒

- 规则不是能力。
- 文档不是证据。
