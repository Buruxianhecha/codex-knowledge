# .codex-instructions v4 给未来 AI 的自举与维护指南

## 第一次打开

依次读：`SYSTEM.md` -> `preferences/user-profile.md` -> `memory/distilled-memory.md` -> `KNOWLEDGE_INDEX.md` -> 两个月审计 -> `FRESHNESS.md / QUALITY.md / .value-rules.md`。

单文件上下文用 `KNOWLEDGE_BUNDLE.md`；文件数有限时用 Compact，但先核对其源版本。

## 开始任务前

- 明确最终拥有者和完成层级。
- 提取“只/仅/不要/单文件/保持原样”等排他约束。
- API/模型/Provider/平台/限额/安全等时效事实先重验。
- 查历史项目、错误、决策和替代关系。
- 高复杂度任务先画依赖 DAG；独立节点可多智能体并行，共享 Schema 后统一审计。

## 执行与交付

- 修改现有项目时保护未指定内容并做负向 diff。
- 凭据、登录态和具体本机用户名路径不进公开知识库。
- 浏览器持久状态按数据库处理。
- CI/测试/部署结论绑定具体版本/SHA。
- 交付报告验证内容、测试类型、目标版本、实际用户路径和未验证项。

## 生命周期与维护

Lesson 2+ 独立证据；Pattern 2+ 独立成功项目；Best practice 3+ 成功应用 + 3个月稳定 + 可执行验收。

每次重大写入同步 Index/Memory/Bundle/Compact，运行 `scripts/validate_knowledge.py`；推送后回读 HEAD、关键文件和当前 HEAD Knowledge Audit CI。
