# 绝对路径优先与运行时检查流程

## 流程

1. 读取自动化记忆或任务上下文，确认上次结果。
2. 用绝对路径读取 `RTK.md`、仓库和目标文件，不依赖当前目录。
3. 验证当前 shell 里相关命令是否真的存在。
4. 如果命令缺失，立即切回原生命令或备用方案。
5. 只修改本次任务相关文件，避免碰触已有脏改动。

## 何时使用

- 当前工作目录不稳定。
- 环境变量可能为空或未导出。
- shell 命令、插件或 GUI 自动化依赖容易漂移。

## 结果

- 提高恢复能力。
- 降低路径和环境假设导致的返工。
