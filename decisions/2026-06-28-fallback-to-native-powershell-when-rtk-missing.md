# 当 `rtk` 缺失时回退到原生 PowerShell

## 决策

在本机 shell 中如果 `rtk` 不可用，就不再围绕它重试，直接使用原生 PowerShell 完成任务。

## 原因

- 当前环境已经证明 `rtk` 不存在。
- 继续等待或假设可恢复，没有实际收益。
- 原生 PowerShell 可以满足本次日记、检查和文件写入需求。

## 后果

- 工作流更稳。
- 结果可立即落地。
- 后续只需在必要时再补充环境修复，而不是阻塞交付。
