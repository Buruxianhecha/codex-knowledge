# Core Knowledge 压缩版 v5

## 最高优先级

未验证 = 未完成；用户实际优先；结论不超过证据；排他需求是合同；保护式修改；行为时间按有效状态；浏览器状态按数据库；敏感信息不入公开库。

## 生命周期

Lesson 2+ 独立证据；Pattern 2+ 独立成功项目；Best practice 3+ 成功应用 + 3个月稳定 + 可执行验收。2026-08-26 已把多引擎/SQLite migration/质量门控从 verified 降为 active；06-22 绝对路径模式由 06-28 运行时检查模式替代。

## 近期工程

- 卡塞尔/剑来/凡人：状态机、单调存档、动作语义、VM/smoke/CI；测试深度分开表述。
- 拾光：IndexedDB + owner 隔离 + Auth Provider；仍是测试版。
- 文章货架：`route && visible && focused && !idle && session` 才累计，chunk 幂等、多标签页防双算。
- BIM：reference plane -> size -> offset -> local addition -> scope -> 3D/断面复核。
- 约束式编辑：Must change / Must preserve / May adapt + 负向 diff。

## 写作研究

材料账本 + 命题表 + 正反证据 + 允许措辞/禁止升级 + 引用审计；多智能体按证据职责拆分，共享 Schema 后统一裁决。

## 自动审计

Knowledge Audit 检查统计、生命周期、断链、Compact 源版本、本机用户名路径和 secret 模式。`error=0` 是硬门槛，legacy warning 单独偿还。
