# 高低价值内容自动判断规则 v3

> 每次会话、项目和写作任务结束时执行。目标是保留可复用决策和证据，不堆积流水账。

## 一、总是记录 P0

- 用户明确表达或纠正的偏好、禁止项和验收标准。
- 完成项目的架构、验证范围和未闭环项。
- Debug 超过 15 分钟的根因与失败信号。
- 技术选型与替代方案比较。
- 用户明确说“学到了、下次注意、重来”的经验。
- 误报完成、隐私泄露、鉴权遗漏和数据丢失风险。
- 写作中被纠正的事实、夸大叙事、证据冲突和资料边界。
- 被 2 个以上项目成功应用的模式，并评估升级 `verified`。

## 二、通常记录 P1

- 可复用代码、检查清单和迁移方法。
- 环境、部署、CI、GitHub 远端核验经验。
- 测试类型与声明边界。
- 材料账本、命题表、引用审计和多智能体研究分工。
- 时序数据的单位、时间、盘中/收盘和修订口径。
- 用户稳定的编码、写作和成本偏好。

## 三、偶尔记录 P2

- 工具链变化和长期项目想法。
- 单次图表或文档中可迁移的方法。
- 没有完成证据但未来可能恢复的项目，只在总审计留痕。

## 四、不进入核心层

- 一次性命令和普通闲聊。
- 基础语法、Git 词表等公开文档可直接查询的信息。
- 项目特定密钥、token、cookie、登录态和私密配置值。
- 只有请求、没有产物/验证/可复用决策的项目。
- 单点数值且没有方法、口径或后续用途。
- 与现有条目重复、没有新增证据的摘要。

不进入核心层不等于删除历史。必要时在时间审计中标记“未提升”。

## 五、证据门槛

| 要声明 | 最低证据 |
|--------|----------|
| 文件已生成 | 可读取产物 |
| 已上传 | 远端回读文件/Blob/commit |
| CI 通过 | 当前目标 SHA 的成功 run |
| 已部署 | 服务端/站点真实入口可访问 |
| 功能可用 | 实际执行核心用户路径 |
| 正式上线 | 安全、恢复、容量、真机和运维边界通过 |
| 研究事实成立 | 命题有可定位来源并通过冲突检查 |
| 因果/领导/法律认定 | 有支持该强动词的直接证据 |

证据不足时可以入库为失败或未闭环，但状态必须真实。

## 六、交叉引用

### 新经验到旧错误

经验解释旧错误时，双方互链；错误文件写根因和预防。

### 新决策到旧经验

决策验证旧经验时更新 `verified_in/reuse_count/last_used`。推翻时使用 `superseded` 和替代路径。

### 新项目到旧模式

项目应用模式时增加应用案例。2 个以上独立项目成功使用后评估 `verified`。

### 新写作到证据模式

研究项目关联材料账本、命题校准和引用清单；发现错误叙事时在项目与经验中保留裁决依据。

## 七、元数据

新核心条目至少包含：

```yaml
---
status: active | verified | best_practice | deprecated | superseded | archived
confidence: 0.0-1.0
reuse_count: 0
last_used: 2026-08-26
verified_in: []
expires_after: none
cross_refs: []
---
```

变化快的技术必须有 `expires_after`。未验证项目的高 confidence 只能表示“对未完成状态判断有信心”，不能表示项目成功。

## 八、周期审查

每 5 个项目或每月检查：

- 条目是否仍适用。
- 错误是否再次发生。
- 模式是否被替代。
- 决策后果是否已有新证据。
- 是否存在重复条目。
- 索引、README、memory、bundle 和实际文件数是否一致。
- 历史条目是否缺元数据。

## 九、会话结束清单

```text
[ ] 新用户偏好或纠正？
[ ] 可复用经验或模式？
[ ] 失败是否记录真实验证边界？
[ ] 技术决策是否写替代方案和代价？
[ ] 写作是否记录来源、冲突和措辞边界？
[ ] 是否有状态升级或替代？
[ ] 是否更新索引和蒸馏记忆？
[ ] 是否扫描凭据和私人元数据？
[ ] 远端提交是否回读验证？
```
