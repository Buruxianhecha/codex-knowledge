# 高/低价值内容自动判断规则 v2

> 给 AI 自己的判断标准。每次会话结束、每个项目完成时，自动执行。

---

## 一、价值判断

### 总是备份 (P0)

- [ ] 用户明确表达的偏好和原则
- [ ] 完成的项目总结（含架构决策）
- [ ] Debug 超过 15 分钟的 bug 及根因
- [ ] 技术选型中的决策对比
- [ ] 用户说"学到了"、"下次注意"的经验
- [ ] 被 2+ 次成功应用的模式 → 升级状态为 `verified`

### 通常备份 (P1)

- [ ] 新发现的编码习惯
- [ ] 可复用的代码片段
- [ ] 备选方案对比（选了 A，为什么没选 B）
- [ ] 环境配置最佳实践
- [ ] 性能优化过程

### 偶尔备份 (P2)

- [ ] 工作流改进
- [ ] 工具链变更
- [ ] 长期项目想法

### 不备份

- [ ] 一次性命令
- [ ] 基础语法问答
- [ ] 闲聊
- [ ] 公开文档可查的信息
- [ ] 项目特定配置的**值**（模板本身可以）
- [ ] 已被新决策覆盖的旧信息 → 标记 `superseded` 而非删除

---

## 二、交叉引用规则

写入新条目时，**必须**检查关联：

### 新经验 → 旧错误
```
这条经验是否解释了之前某个错误的根因？
→ 在错误文件底部加 "# 根因追溯: lessons/xxx.md"
```

### 新决策 → 旧经验
```
这个决策是否验证或推翻了已有经验？
→ 验证: 经验状态升级为 verified
→ 推翻: 经验状态标记为 superseded，指向新决策
```

### 新项目 → 旧模式
```
项目是否成功应用了已有模式？
→ 在模式文件底部加 "# 应用案例: projects/xxx.md"
→ 2+ 次应用 → 模式状态升级为 verified
```

### 新错误 → 旧决策
```
错误是否可追溯到某个设计决策？
→ 在错误文件标注 "# 决策根因: decisions/xxx.md"
→ 在决策文件标注 "# 已知后果: mistakes/xxx.md"
```

---

## 三、知识演化规则

### 周期性审查（每 5 个项目或每月）

| 检查 | 操作 |
|------|------|
| 经验是否仍适用？ | 仍适用 → 保持 active；不适用 → 标记 outdated |
| 错误是否仍在犯？ | 仍在犯 → 加粗警示；已避免 → 标记 "已内化" |
| 模式是否被新方案替代？ | 有更好的 → 旧模式标记 superseded，指向新模式 |
| 决策后果是否已知？ | 新信息 → 更新决策文件的"后果"部分 |
| 有合并机会吗？ | 两个条目说同一件事 → 合并，旧条目标记 archived |

### 状态标记格式

在每个文件头部：
```markdown
---
status: active | verified | superseded | outdated | archived
since: 2026-05-25
verified_in: [pdf-to-excel, another-project]  # 仅 verified 状态
superseded_by: patterns/better-pattern.md      # 仅 superseded 状态
---
```

### 不删除，只标记

错误的旧知识对理解演化路径有价值。永远不删除——标记 `outdated` 或 `archived`，并指向替代方案。

---

## 四、会话结束检查清单

```
[ ] 有新用户偏好？ → preferences/user-profile.md
[ ] 有可复用经验？ → lessons/ + 交叉引用到 mistakes/decisions
[ ] 有值得记录的 bug？ → mistakes/ + 根因追溯到 decisions
[ ] 有技术决策？ → decisions/ + 关联已有经验
[ ] 有代码模板？ → templates/
[ ] 有经验被验证/推翻？ → 更新状态标记
[ ] 更新 KNOWLEDGE_INDEX.md
[ ] 更新 memory/distilled-memory.md
```
