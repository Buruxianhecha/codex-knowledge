# 高低价值内容自动判断规则 v4

> 保留可复用决策和证据，不堆积流水账，也不让派生分享层悄悄落后。

## 总是记录 P0

用户明确偏好/禁止项/验收标准；项目验证范围和未闭环项；关键根因与失败信号；技术选型；重复错误；误报完成、隐私、鉴权和数据丢失风险；写作证据冲突和措辞边界。

## 通常记录 P1

可复用代码/清单/迁移；环境/部署/CI/远端核验；测试类型与声明边界；材料/命题/引用审计/多智能体分工；时序数据口径；稳定工程与写作偏好。

## 不进入核心层

一次性命令/普通闲聊；公开文档可直接查询且无方法增量的基础事实；密钥/token/cookie/登录态；只有请求没有产物/验证/可复用决策的项目；重复摘要。

## 生命周期

Lesson 两个独立证据上下文后评估 verified；Pattern 两个独立成功项目后评估 verified；best_practice 三个成功应用 + 三个月稳定 + 可执行验收。被替代知识标 `superseded/replaced_by`。

## 重大写入必须同步

1. `KNOWLEDGE_INDEX.md`
2. `memory/distilled-memory.md`
3. `KNOWLEDGE_BUNDLE.md`
4. `README.md`（重大增量/统计变化）
5. 两套 Compact（主知识版本变化时）
6. `python3 scripts/validate_knowledge.py`
7. 推送后回读 HEAD 与当前 HEAD 的 Knowledge Audit CI

历史元数据 warning 可以分批偿还，但生命周期/断链/隐私/统计/Compact 漂移属于硬错误。
